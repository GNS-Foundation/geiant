[phases.setup]
nixPkgs = ['nodejs_18', 'npm-9_x']

[phases.install]
cmds = ['npx -y pnpm@9 install --no-frozen-lockfile']

[phases.build]
cmds = []

[start]
cmd = 'node --import tsx/esm src/index.ts'

{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd packages/mcp-agentcore && npm install && npx tsc"
  },
  "deploy": {
    "startCommand": "node packages/mcp-agentcore/dist/index.js",
    "healthcheckPath": "/health"
  }
}
